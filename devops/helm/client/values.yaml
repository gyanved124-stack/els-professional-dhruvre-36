replicaCount: 1

image:
  repository: dhruvre/devops-client # DOCKER_USERNAME
  pullPolicy: Always
  tag: "871065e2351c59905c3b8d8e55137b8aab0e2153" # IMAGE_TAG

config:
  apiUrl: "http://els-server.local/api" # Update with Minikube IP
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 80

ingress:
  enabled: true
  className: "nginx"
  annotations: {}
  hosts:
    - host: els-client.local
      paths:
        - path: /
          pathType: Prefix

# Server URL that will be passed to React app at build time
serverUrl: "http://els-server.local"

env: {}
