apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "els-client.fullname" . }}-config
  labels:
    {{- include "els-client.labels" . | nindent 4 }}
data:
  config.js: |
    window.env = {
      API_URL: "{{ .Values.config.apiUrl }}"
    };
