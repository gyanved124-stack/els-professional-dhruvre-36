# Backend API URL
# ⚠️ IMPORTANT: For Kubernetes deployments, leave VITE_API_URL empty!
# The API URL will be loaded from /config.js (mounted via ConfigMap) at runtime
# This allows changing the API URL without rebuilding the Docker image

# For local development only (uncomment ONE option):
# Option 1: Express REST API (local development, no database required)
# VITE_API_URL=http://localhost:5000/api

# Option 2: Local Strapi CMS
# VITE_API_URL=http://localhost:1337/api

# Option 3: Remote Strapi instance
# VITE_API_URL=http://202.38.182.170:1348/api

# For Kubernetes: Leave empty - runtime config takes precedence
VITE_API_URL=
